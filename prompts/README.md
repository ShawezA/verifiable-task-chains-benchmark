# Prompts and Verifiers

This directory contains all prompts and verifiers used in the benchmark.

- Each verifier is stored in a Jupyter Notebook to keep the unit test suite
  for each prompt clearly separated.
- The actual test harness selects and runs the tests for each prompt
  automatically and **does not** use these files directly.

These notebooks are provided for documentation and reference purposes only.